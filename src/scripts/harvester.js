
 export const harvestPlants = (plantArrayPlaceholder) => {
    let seedArray = []

     for (const row of plantArrayPlaceholder) {
        for (const plant of row) {
             let output = plant.output.value
             console.log(output)
            }
    
     //return seedArray
    }
}