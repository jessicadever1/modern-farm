

export const plantSeeds = (yearlyPlan) => {
    let plants = [];
    for (let i = 0; i < yearlyPlan.length; i++) {
            for(j = 0; j < yearlyPlan[i].length; j++) {
            
            }
    }
    
    return plants

}

const returnValueOfPlantSeeds = plantSeeds(activatePlan)
